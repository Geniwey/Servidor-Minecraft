name: Servidor Minecraft 24/7

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

concurrency:
  group: servidor-minecraft
  cancel-in-progress: true

jobs:
  iniciar_servidor:
    runs-on: ubuntu-latest
    steps:
      - name: Preparar Java (JAVA 21)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Instalar Rclone (Conexión a Drive)
        run: |
          curl https://rclone.org/install.sh | sudo bash
          mkdir -p ~/.config/rclone
          cat << 'EOF' > ~/.config/rclone/rclone.conf
          ${{ secrets.RCLONE_CONF }}
          EOF

      - name: Descargar el mundo y llave
        run: |
          rclone copy gdrive:playit.toml ./ || true
          rclone copy gdrive:ZerdolandIII.tar.gz ./ || true
          
          if [ -f "ZerdolandIII.tar.gz" ]; then
            tar -xzf ZerdolandIII.tar.gz
            rm ZerdolandIII.tar.gz
          else
            rclone copy gdrive:ZerdolandIII ./servidor --transfers 8
          fi

      - name: Instalar y configurar Playit
        run: |
          curl -SsL https://playit-cloud.github.io/ppa/key.gpg | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/playit.gpg >/dev/null
          echo "deb [signed-by=/etc/apt/trusted.gpg.d/playit.gpg] https://playit-cloud.github.io/ppa/data ./" | sudo tee /etc/apt/sources.list.d/playit-cloud.list
          sudo apt update
          sudo apt install playit
          sudo mkdir -p /etc/playit
          
          if [ -f "playit.toml" ]; then
            sudo cp playit.toml /etc/playit/playit.toml
            sudo chmod 777 /etc/playit/playit.toml
          fi

      - name: Arrancar Playit y Servidor
        run: |
          sudo playit > playit.log 2>&1 &
          
          # EL AUTO-GUARDADO EN VIVO
          (
            while true; do
              LINK=$(grep -m 1 -oE 'https://playit.gg/claim/[a-zA-Z0-9]+' playit.log || true)
              IP=$(grep -m 1 -oE '[a-zA-Z0-9-]+\.(gl\.joinmc\.link|auto\.playit\.gg)' playit.log || true)
              
              # LA MAGIA: Fuerza la subida a Drive cada 30 segundos
              if [ -f "/etc/playit/playit.toml" ]; then
                 sudo cp /etc/playit/playit.toml ./playit.toml || true
                 sudo chmod 777 ./playit.toml || true
                 rclone copy ./playit.toml gdrive: || true
              fi
              
              echo ""
              echo "🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴"
              if [ -n "$IP" ]; then
                echo "✅ TU IP: $IP"
                echo "💾 (Tu llave se está guardando en Drive automáticamente)"
              elif [ -n "$LINK" ]; then
                echo "⚠️ FALTA VINCULAR PLAYIT. CLICA AQUÍ YA: $LINK"
              else
                echo "⏳ Esperando..."
              fi
              echo "🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴"
              echo ""
              sleep 30
            done
          ) &
          
          cd servidor
          echo "eula=true" > eula.txt
          chmod +x run.sh
          ./run.sh

      - name: Guardar mundo en Drive
        if: always()
        run: |
          echo "Empaquetando el servidor..."
          tar -czf ZerdolandIII.tar.gz servidor
          rclone copy ZerdolandIII.tar.gz gdrive:
